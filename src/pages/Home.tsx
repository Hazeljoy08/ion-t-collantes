import React from 'react';
import { IonHeader, IonToolbar, IonTitle } from '@ionic/react';

const Home: React.FC = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>Home</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Home;
